<h2 style="font-size: 1.3rem; margin-bottom: 25px;">Langganan</h2>

<% if (results && results.length> 0) { %>
    <div
        style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; text-align: center; margin-bottom: 30px;">
        <% results.forEach(sub=> { %>
            <div onclick="navigate('/?q=<%= encodeURIComponent(sub.uploader) %>')" style="cursor: pointer;">
                <div
                    style="width: 60px; height: 60px; border-radius: 50%; background: var(--surface-accent); margin: 0 auto 8px; display: flex; align-items: center; justify-content: center; border: 2px solid var(--primary);">
                    <span style="font-size: 1.2rem; font-weight: 700; color: white;">
                        <%= sub.uploader.charAt(0).toUpperCase() %>
                    </span>
                </div>
                <p
                    style="font-size: 0.65rem; font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; margin: 0;">
                    <%= sub.uploader %>
                </p>
            </div>
            <% }); %>
    </div>
    <% } else { %>
        <div style="text-align: center; padding: 100px 20px; opacity: 0.5;">
            <i class="fas fa-users-slash" style="font-size: 3rem; margin-bottom: 20px; display: block;"></i>
            <p>Belum mengikuti siapapun.</p>
        </div>
        <% } %>

            <div style="border-top: 1px solid rgba(255,255,255,0.05); padding-top: 25px;">
                <h3 style="font-size: 0.95rem; margin-bottom: 15px;">Video Terbaru dari Subs</h3>
                <p
                    style="text-align: center; padding: 40px; color: var(--text-dim); font-size: 0.8rem; background: var(--surface); border-radius: 16px;">
                    SSR: Bagian ini akan otomatis memuat konten terbaru di versi mendatang.
                </p>
            </div>